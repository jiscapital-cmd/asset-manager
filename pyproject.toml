[project]
name = "ai-accelerator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sandeep Pradhan", email = "pradhaninvest@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "openai>=3.3.1",
    "python-dotenv>=1.2.3",
    "streamlit>=1.62.0",
]

[project.scripts]
ai-accelerator = "ai_accelerator:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
