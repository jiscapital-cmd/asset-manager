# Direct dependencies, mirroring pyproject.toml (the source of truth for this
# project). Provided for tools/platforms that expect a requirements.txt
# specifically (some deploy hosts, older pip workflows) rather than reading
# [project.dependencies] from pyproject.toml directly.
#
# Install with: pip install -r requirements.txt
# (equivalent to: pip install -e .)

deepagents>=0.6.12
langchain-openai>=1.3.3
langgraph-cli[inmem]>=0.4.30
chromadb>=0.5
google-api-python-client>=2.140
google-auth>=2.34
pypdf>=5.0
python-docx>=1.1
streamlit>=1.38
python-dotenv>=1.0
reportlab>=4.0
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
requests>=2.32
openpyxl>=3.1

# --- dev / test ---
pytest>=8.0
